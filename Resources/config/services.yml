parameters:
    sinch.service.class: Fresh\SinchBundle\Service\SinchService

services:
    sinch:
        class: "%sinch.service.class%"
        arguments:
            - "@event_dispatcher"
            - "%sinch.host%"
            - "%sinch.key%"
            - "%sinch.secret%"
            - "%sinch.from%"
